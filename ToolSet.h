#pragma once
#define __throwif__(expression,notification) if(expression){throw notification;}//read as rap god lmao
#define forever while(true)
#define ever (;;) // for ever
#define repeat(count) for(unsigned int p3xe_i = 0; p3xe_i < (count); ++p3xe_i)
#define foreach(key, mass) for(unsigned int i = 0; i < mass.length; i++)
